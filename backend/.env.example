# Copy to .env and fill in. Never commit .env (NFR-11 / invariant 16).

DATABASE_URL=sqlite:///./data/route53.db
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGINS=http://localhost:3000
DEMO_MODE=true
