# Copy to .env.local and fill in. Never commit .env* (NFR-11 / invariant 16).

# Server-side only — used in next.config.ts rewrites, never exposed to the browser.
BACKEND_ORIGIN=http://localhost:8000

# Exposed to the browser (login page shows seeded demo credentials).
NEXT_PUBLIC_DEMO_MODE=true
