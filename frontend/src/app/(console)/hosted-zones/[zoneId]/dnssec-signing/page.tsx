import { ComingSoon } from "@/components/ComingSoon";

// [DERIVED — A2]: same "Coming Soon" treatment as the other placeholder sections
// (07-ui-spec.md, "What we added beyond the brief").
export default function DnssecSigningTabPage() {
  return <ComingSoon />;
}
